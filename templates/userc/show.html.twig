{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Nom_user</th>
                <td>{{ user.nomUser }}</td>
            </tr>
            <tr>
                <th>Prenom_user</th>
                <td>{{ user.prenomUser }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ user.mail }}</td>
            </tr>
            <tr>
                <th>Mdp</th>
                <td>{{ user.mdp }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('userc_index') }}">back to list</a>

    <a href="{{ path('userc_edit', {'id': user.id}) }}">edit</a>

    {{ include('userc/_delete_form.html.twig') }}
{% endblock %}
